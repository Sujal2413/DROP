# User Preferences
- DO NOT start development servers or background processes that listen on ports. The user prefers to run their own dev servers and manage ports manually. Never run `npm run dev` or similar commands in the background.
